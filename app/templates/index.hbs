<div class="row">
  <div class="col-md-8">
    <h2>{{siteTitle}}</h2>
    <p>It is {{currentTime}}</p>

    <ul>
      {{#each}}
        {{question-preview question=this}}
      {{/each}}
    </ul>
  </div>
</div>