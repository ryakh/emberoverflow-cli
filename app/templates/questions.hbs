<div class="row">
  <div class="col-md-6">
    <h2>{{siteTitle}}</h2>
    <p>It is {{current-date "LL"}}</p>

    <h3> Latest questions</h3>

    <ul>
      {{#each}}
        {{question-preview question=this}}
      {{/each}}
    </ul>
  </div>

  <div class="col-md-6">
    {{outlet}}
  </div>
</div>