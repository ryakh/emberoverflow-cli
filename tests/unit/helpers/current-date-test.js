import {
  currentDate
} from 'emberoverflow-cli/helpers/current-date';

module('CurrentDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = currentDate();
  ok(result);
});
